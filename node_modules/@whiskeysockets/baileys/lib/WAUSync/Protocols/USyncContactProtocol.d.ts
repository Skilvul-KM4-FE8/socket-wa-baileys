import { USyncQueryProtocol } from '@whiskeysockets/baileys/lib/Types/USync';
import { BinaryNode } from '@whiskeysockets/baileys/lib/WABinary';
import { USyncUser } from '@whiskeysockets/baileys/lib/WAUSync/USyncUser';
export declare class USyncContactProtocol implements USyncQueryProtocol {
    name: string;
    getQueryElement(): BinaryNode;
    getUserElement(user: USyncUser): BinaryNode;
    parser(node: BinaryNode): boolean;
}
